# Referencia arquitectónica

Colocar aquí el plano maestro del sitio:

```
labor-master-plan.png
```

Es la referencia maestra de **relaciones espaciales** para calibrar
`src/data/map.ts` y las huellas de edificios en `src/data/spaces.ts`.

No se reproduce su estilo gráfico, cotas ni panel de resumen.
